%div
  = form_tag(comments_user_lost_item_path(current_user.id,lost_item),:remote => true,:method => :post) do
    = label_tag "Post your comment"
    = text_area_tag :content,nil,:maxlength => 50, :placeholder => "50 characters only",:class => "comment_form_textarea"
    %br/
    = submit_tag "Submit", :class => 'btn btn-primary'