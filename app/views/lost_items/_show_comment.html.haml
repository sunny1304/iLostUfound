-if comments.present?
  -comments.each do |comment|
    %div{:class => "alert", :id => "comment_#{comment.id}",:style => "width:300px"}#{comment.user_email} says ..<br/>#{comment.content}
  = paginate comments, :remote => true
-else
  %p No comments yet    	