%p#notice= @notice
- if @suggestion_items.present?
	%p The item you reported might be lost by any of these people :
	- @suggestion_items.each do |lost_item|
		%br/
		%div{:class=> "lost_item_suggestion",:id => "lost_item_suggestion_#{lost_item.id}",:style=>"height:60px"}= link_to "#{lost_item.name unless lost_item.name.blank?} has lost #{check_vowel(lost_item.lost_item)} #{process_item_name(lost_item.lost_item) unless lost_item.lost_item.blank?} near #{lost_item.lost_location} at #{lost_item.lost_date.strftime('%d %B, %Y') unless lost_item.lost_date.blank?}", user_lost_item_path(lost_item.user_id,lost_item.id)
		%br/
- else
	%p No one has reported yet the item you found. 		



:javascript
	$(function(){
		$("#notice").fadeOut(1500);		
	});
